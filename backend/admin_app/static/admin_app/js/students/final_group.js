const csrfToken = document.querySelector('meta[name="csrf-token"]').content;

export function finaliseGroup(groupId, name, notes) 
{
    // Placeholder
    console.log("Finalising group:", groupId, name, notes);
}